# 🛠️ AsyncPythonProjectTemplate
## 🌐 Обзор

**AsyncPythonProjectTemplate** — это базовый шаблон асинхронного Python-приложения, который создан для ускорения разработки реальных проектов. Этот проект сочетает в себе современные подходы к разработке, использование асинхронного программирования и модульную архитектуру, позволяя разработчикам сосредоточиться на бизнес-логике вместо рутинной настройки инфраструктуры.

---

### 📋 **Из чего состоит проект?**

Проект построен на модульной архитектуре, которая разделяет функциональность на независимые и легко расширяемые модули. Каждый модуль отвечает за свою область ответственности и управляется через централизованный контейнер `AppContainer`. 

- **`AppContainer`** — отвечает за управление жизненным циклом приложения, конфигурацию модулей, запуск и завершение работы.
- **Модули:**
  - **`DatabaseManager`**: Управляет подключением к базе данных через [`SQLAlchemy`](https://www.sqlalchemy.org/) (асинхронный движок).
  - **`ApiClientManager`**: Обеспечивает взаимодействие с внешними API на основе [`aiohttp`](https://docs.aiohttp.org/).
  - **`AdminManager`**: Настраивает административный интерфейс на основе [`Starlette Admin`](https://jowilf.github.io/starlette-admin/).
  - **`BotManager`**: Управляет Telegram-ботом с использованием [`aiogram`](https://docs.aiogram.dev/).
  - **`ApiManager`**: Оборачивает API-приложение, построенное на [`FastAPI`](https://fastapi.tiangolo.com/).
  - **`SchedulerManager`**: Отвечает за управление асинхронными задачами и крон-джобами через [`APScheduler`](https://apscheduler.readthedocs.io/).
  - **`ServerManager`**: Управляет запуском [`Uvicorn`](https://www.uvicorn.org/) сервера для обработки HTTP-запросов.
- **Конфигурация:**
  - Используется [`pydantic-settings`](https://pypi.org/project/pydantic-settings/) для структурированной и строгой валидации настроек приложения.
  - Настройки централизованы в одном месте (`config.py`) и поддерживают загрузку из переменных окружения.
- **Асинхронное программирование:**
  - Все ключевые операции используют асинхронный подход (`asyncio`), что делает проект высокопроизводительным и подходящим для обработки большого числа соединений.
- **Обработка сигналов:**
  - Реализована система корректного завершения работы через обработку сигналов (`SIGINT`, `SIGTERM`).
  - Фоновые задачи и сервер завершаются безопасно, с освобождением всех ресурсов.
- **Логирование:**
  - Реализована система логирования с записью в файл и доставкой логов уровня `WARNING` и выше в Telegram через [`aiogram`].
  - Это позволяет оперативно уведомлять разработчиков о критических ошибках и предупреждениях.

---

### 💡 **Преимущества**

1. **Модульная архитектура**:
   - Каждый модуль реализован в виде отдельного класса (`Manager`) с методами `configure`, `start` и `shutdown`, что упрощает тестирование, поддержку и расширение функциональности.
   - Лёгкость интеграции новых модулей.

2. **Асинхронное программирование**:
   - Использование `asyncio` и асинхронных библиотек (например, `aiogram`, `FastAPI`, `aiohttp`) позволяет максимально эффективно использовать ресурсы системы.

3. **Гибкость конфигурации**:
   - Все настройки приложения централизованы, и их можно легко адаптировать под разные окружения (локальная разработка, тестирование, продакшен).

4. **Управление жизненным циклом**:
   - Реализован централизованный контроль над всеми процессами приложения через `AppContainer`.
   - Поддерживается плавное завершение работы (graceful shutdown), что гарантирует сохранение данных и завершение фоновых задач.

5. **Современные технологии**:
   - Используются лучшие практики Python-разработки:
     - `FastAPI` для создания API.
     - `SQLAlchemy` для работы с базой данных.
     - `aiogram` для разработки Telegram-ботов.
     - `aiohttp` для взаимодействия с внешними API.
     - `APScheduler` для управления задачами и крон-джобами.
     - `Uvicorn` для высокопроизводительного ASGI-сервера.

6. **Готовность к масштабированию**:
   - Проект построен с учётом возможности добавления новых модулей, что позволяет адаптировать его для различных сценариев — от небольших проектов до более сложных приложений.

7. **Простота использования**:
   - Проект предназначен как для быстрого старта разработки, так и для долгосрочного использования в приложениях, которые требуют поддержки модульной архитектуры.

---

<!-- ### 🚀 **Кому будет полезен этот проект?**

- **Python-разработчикам**, которые хотят быстро стартовать проект на основе асинхронных технологий.
- **Командам разработки**, ищущим шаблон с модульной архитектурой и современным стеком технологий.
- **Начинающим разработчикам**, желающим изучить архитектуру продакшен-приложений.
- **HR-менеджерам** и **техническим специалистам**, чтобы увидеть пример моего кода, демонстрирующий уровень владения Python и знание современных технологий. -->
<!-- Мне:) Хехе-->

---

### 🌟 **Почему выбрать этот проект в качестве основы?**

- Этот проект создан с учётом реальных требований и проблем, возникающих в процессе разработки.
- Он демонстрирует современные подходы к проектированию архитектуры Python-приложений.
- Код легко читается, модифицируется и поддерживается.
- Этот шаблон может служить стартовой точкой как для небольших решений, так и для адаптивных приложений.



## 📋 Оглавление
- [Функциональность](#-функциональность)
- [Установка и запуск](#️-установка-и-запуск)
- [Стек технологий](#-стек-технологий)
- [Структура проекта](#-структура-проекта)
- [Переменные окружения](#-переменные-окружения)

## 🚀 Функциональность
### 1. Команды бота:  

/start - запуск бота  

### 2. Первая фича:  

**С такими-то опциями**:  
  
Описание
  
**С другими опциями**:  
  
Описание  

### 3. Вторая фича:  
  
Описание  

...  

### 6. Мониторинг и логирование:  

Интеграция с лог-ботом для администрирования ошибок.

## ⚙️ Установка и запуск

1. **Клонирование репозитория**:
   ```bash
   git@github.com:petra-khrushcheva/base_project.git
   cd base_project
   ```

2. **Настройка переменных окружения: создайте файл .env на основе [.env.example](./.env.example) и заполните его необходимыми данными.**

3. **Запуск Docker Compose:**

   ```bash
   docker compose -f docker-compose-dev.yml up
   ```

4. **Запуск приложения и сервера: Приложение автоматически запускается при старте Docker Compose.**

5. **Создание аккаунта для доступа к админ-панели:**

   После того, как контейнеры будут запущены, необходимо создать администратора для доступа к админ-панели. Для этого выполните следующие шаги:

   1. Зайдите в контейнер с помощью команды:
      ```bash
      docker exec -it <container_name> sh
      ```
      Пример:
      ```bash
      docker exec -it base_project_backend sh
      ```

   2. Создайте администратора, используя команду:
      ```bash
      python src/cli.py create-admin <admin_name> <admin_password>
      ```
      Пример:
      ```bash
      python src/cli.py create-admin admin password123
      ```
      В результате должно появиться сообщение:
      ```
      New admin created: <admin_name>
      ```
   3. После успешного создания администратора, выйдите из контейнера:
      ```bash
      exit
      ```

   Админка доступна по адресу /admin
   
6. **Создание и применение миграций:**

   Для внесения изменений в структуру базы данных (например, добавление новых колонок, таблиц и т.д.), нужно создать миграцию и применить её. Для этого при запущенных контейнерах выполните следующие шаги:

   1. Зайдите в контейнер с помощью команды:
      ```bash
      docker exec -it <container_name> sh
      ```
      Пример:
      ```bash
      docker exec -it base_project_backend sh
      ```

   2. Создайте миграцию с помощью команды:
      ```bash
      alembic revision --autogenerate -m "<название_миграции>"
      ```
      Пример:
      ```bash
      alembic revision --autogenerate -m "add_some_column_to_some_table"
      ```

   3. Выйдите из контейнера:
      ```bash
      exit
      ```

   4. Перезапустите контейнеры для автоматического применения миграции:
      ```bash
      docker compose -f docker-compose-dev.yml up
      ```


## 🛠 Стек технологий
- **Python**: язык программирования
- **Aiogram**: для создания и управления телеграм-ботом
- **FastAPI**: для управления приложением
- **Aiohttp**: для асинхронных HTTP-запросов
- **PostgreSQL**: реляционная база данных
- **SQLAlchemy**: ORM для взаимодействия с базой данных
- **Redis**: для хранения состояний бота и кэширования данных
- **Alembic**: для управления миграциями базы данных
- **Starlette Admin**: для создания админ-панели
- **Docker**: для контейнеризации
<!-- - **S3-совместимое хранилище**: для хранения и получения медиафайлов -->


## 📁 Структура проекта
```bash
base_project/
├── .github/workflows/             # Конфигурация для CI/CD в GitHub
├── alembic/                       # Файлы миграций
├── src/
│   ├── admin/                     # Настройка админ-панели
│   ├── api/                       # Эндпоинты FastAPI
│   ├── api_client/                # Клиенты для взаимодействия с внешними API и сервисами
│   ├── bot/                       # Логика телеграм-бота
│   ├── core/                      # Общие компоненты и базовые классы, используемые в проекте
│   ├── database/                  # Модели и репозитории базы данных
│   ├── scheduled_jobs/            # Периодические задачи
│   ├── server/                    # Настройка и управление Uvicorn-сервером
│   ├── services/                  # Бизнес логика 
│   ├── utils/                     # Вспомогательные модули
│   ├── app_container.py           # Управление жизненным циклом приложения (инициализация, конфигурация, запуск, завершение)
│   ├── cli.py                     # Скрипт для выполнения команд через CLI
│   ├── config.py                  # Конфигурация приложения
│   ├── main.py                    # Основной файл для запуска приложения
│   └── schemas.py                 # Схемы данных
├── .env.example                   # Шаблон файла переменных окружения
├── alembic.ini                    # Конфигурация Alembic для миграций
├── docker-compose-dev.yml         # Docker Compose для разработки
├── docker-compose.yml             # Docker Compose для продакшена
├── Dockerfile                     # Dockerfile для сборки образа
└── requirements.txt               # Список зависимостей проекта
```

## 🔑 Переменные окружения

### PostgreSQL
- `POSTGRES_HOST`: адрес сервера базы данных
- `POSTGRES_PORT`: порт сервера базы данных
- `POSTGRES_DB`: название базы данных
- `POSTGRES_USER`: пользователь базы данных
- `POSTGRES_PASSWORD`: пароль пользователя базы данных

### Redis
- `REDIS_HOST`: адрес сервера Redis
- `REDIS_PORT`: порт сервера Redis
- `REDIS_DB`: номер базы данных Redis (по умолчанию 0)

### Настройки внешних сервисов
- `SOME_API_URL`: адрес первого внешнего API
- `SOME_API_KEY`: ключ доступа к первому внешнему API 
- `SOME_API_SECRET`: секретный ключ доступа к первому внешнему API (если требуется)
- `SOME_OTHER_API_URL`: адрес второго внешнего API 
- `SOME_OTHER_API_KEY`: ключ доступа ко второму внешнему API
- `SOME_OTHER_API_SECRET`: секретный ключ доступа ко второму внешнему API (если требуется)

### Настройки телеграм бота
- `BOT_TOKEN`: токен бота  

### Настройки логирования
- `LOG_BOT_TOKEN`: токен бота для логов  
- `MAINTAINERS_USER_IDS`: список Telegram ID получателей логов бота

### Безопасность
- `SECRET_KEY`: ключ для шифрования и защиты сессий
- `FORWARDED_ALLOW_IPS`: список IP-адресов, которым разрешено передавать заголовок X-Forwarded-For, используемый для доступа к админке через прокси-сервер

### Сервер
- `SERVER_HOST`: адрес хоста сервера (по умолчанию 0.0.0.0)
- `SERVER_PORT`: порт сервера (по умолчанию 8000)

<!-- ### Настройки S3
- `AWS_ACCESS_KEY_ID`: идентификатор ключа доступа AWS
- `AWS_SECRET_ACCESS_KEY`: секретный ключ доступа AWS
- `S3_BUCKET`: имя S3-бакета
- `S3_ENDPOINT_URL`: URL-адрес API для взаимодействия с S3-совместимым сервисом
- `S3_DOMAIN`: URL-адрес для доступа к объектам -->

Подробнее — в файле [`.env.example`](./.env.example).